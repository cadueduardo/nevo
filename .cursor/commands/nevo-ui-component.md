# nevo-ui-component

Create a frontend component for the Nevo Admin UI.
Requirements:
- TypeScript
- clear props and state
- no business logic in UI
- data fetched via Supabase client
- include loading and error states

This command will be available in chat with /nevo-ui-component
